import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  btn: {},
  inp: {
    borderColor: '#80b0e8',
    borderWidth: 2,
    padding: 10,
    borderRadius: 5,
  },
});
